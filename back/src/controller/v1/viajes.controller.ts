import { Request, Response } from "express";
import { prisma } from "../../config/dataSource.js";

export function getViajes(req: Request, res: Response) {
}

export function getViaje(req: Request, res: Response) {

}
